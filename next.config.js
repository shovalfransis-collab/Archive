/** @type {import('next').NextConfig} */
// This file configures Next.js itself. We don't need anything special —
// the defaults work fine for this app — but it has to exist.
const nextConfig = {};

module.exports = nextConfig;
